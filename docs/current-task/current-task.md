# 作用域： apps/admin,apps/api

# 目标：报告管理模块，包括查询、逻辑删除功能

# 需求

- 查询功能
  - 查询条件：时间，用户，主题名，类型，查询后分页加载，每页20条
  - 列表分页显示报告的主要信息
- 删除
  - 该次报告可以删除，删除给二次确认，逻辑删除后刷新列表
- 详情
  - 点击详情，能弹框显示报告的详情信息，和web端的报告内容差不多。

# 限制：不要看其他的docs，不用改动apps/web,

# 规范：

- 使用现有的admin端共通组件

# 验证：

- 能根据查询条件查询报告的列表
- 能逻辑删除该次对话
