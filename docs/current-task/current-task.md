# 目标

- 实现用户可以实时语音练习中文对话

# 需求

- 真正接入豆包端到端实时语音
- 把当前内存态会话/报告落到 PostgreSQL + Redis

# 修改范围

# 不要做

- 不用读整个docs下面的文档

# 功能验收标准

- 点击首页的主题对话，跳转到practice页面，可以支持真正的豆包实时语音对话
  - 开始实时对话之前或者结束后，显示角色，也可以让用户选择不同的角色。对话中，则不能选择角色
  - 不用mock数据，要真正的豆包实时语音对话的功能
  - 练习主题都用英文，除了实时语音对话框中的问题，其他网站文案都是英文，用户主要是外国学生
- 把当前内存态会话/报告落到 PostgreSQL + Redis，不用mock数据
