# 作用域： apps/admin,apps/api

# 目标：开发实时对话管理模块，包括查询、逻辑删除

# 需求

- 查询功能
  - 查询条件：时间，用户，主题名，类型，查询后分页加载，每页20条
  - 列表分页显示实时对话的主要信息
- 删除
  - 该次对话可以删除，删除给二次确认，逻辑删除后刷新列表

# 限制：不要看其他的docs，不用改动apps/web,

# 规范：

- 使用现有的admin端共通组件

# 验证：

- 能根据查询条件查询对话主题的列表
- 能逻辑删除该次对话
